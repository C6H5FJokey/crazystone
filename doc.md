
## machine
放置和机器相关的函数
每个机器一个单独的文件夹
机器主要功能有初始化，运行，销毁。
_place_check, _init, _tick, _del
_place_check: 运行后检测运行位置是否能放置机器，如果能，return 1, 否则为0
init: 运行后在运行位置放置一台对应的机器，并执行相关初始化函数，会自动做偏移
_init: 对初始化的机器实体本身做初始化的函数，要求一定要提供binding machine_id_jk
_tick: 每tick机器所执行的代码，因为机器不是每tick都会触发main，但是触发后执行main函数
main: 机器的主要功能函数
_del: 析构函数，由机器执行，执行后销毁该机器。
机器默认是在方块中心(0.5, 0.5, 0.5)的位置，展示实体即为方块本体，除此之外还会有一个附加的判定方块。
默认tag包含machine_jk

协议目前有点混乱
